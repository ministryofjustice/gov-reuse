# Security composition analysis for dependencies and Docker images
# Scans for vulnerabilities, uploads SARIF results to GitHub Security tab
# Trigger: schedule (daily), pull_request, push, workflow_dispatch | Permissions: contents:write, security-events:write

name: SCA ⚡️
run-name: Software composition analysis ⚡️

on:
  schedule:
    - cron: "0 0 * * *"

  pull_request:
    branches: ["**"]

  workflow_dispatch:
  branch_protection_rule:

permissions: {}

<<<<<<< HEAD
jobs:
=======
env:
  environment: base
  timezone: ${{ vars.TIMEZONE }}

concurrency:
  group: sca-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    name: Setup 🔧
    runs-on: ["ubuntu-latest"]
    outputs:
      environment: ${{ env.environment }}
      timezone: ${{ env.timezone }}
    steps:
      - name: Environment 🧪
        run: echo "Environment set to ${{ env.environment }}"

      - name: Timezone 🌐
        run: echo "Timezone set to ${{ env.timezone }}"
>>>>>>> main
  sca:
    permissions:
      contents: write
      pull-requests: write
      issues: write
      security-events: write
      actions: read

    name: Execute ⚙️
    runs-on: ["ubuntu-latest"]

    steps:
      - name: 📦 Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ vars.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ${{ github.workspace }}/package-lock.json

      - name: ⛓️ SLSA
        uses: ministryofjustice/devsecops-actions/sca/slsa@b68f06598355021511df55a612610e7deda935df # v1.5.0

      - name: SLSA
        uses: ministryofjustice/devsecops-actions/sca/slsa@8c77d3a65a46d1d4b5416eafae5b84371ecd797d # v1.5.0

      - name: Install
        shell: bash
        run: npm ci --ignore-scripts

      - name: ⚡️ SCA
        uses: ministryofjustice/devsecops-actions/sca@b68f06598355021511df55a612610e7deda935df # v1.5.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # Mandatory
          dependency-review-config-file: "./.github/dependency-review-config.yml" # Optional
          dependency-check-suppression-file: "dependency-check-suppression.xml" # Optional
